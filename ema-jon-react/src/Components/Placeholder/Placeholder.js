import React from 'react';
import img from "../../images/giphy.gif"

const Placeholder = () => {
    return (
        <div>
            <img className='placeholder' src={img} alt="buy" />
        </div>
    );
};

export default Placeholder;